generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id    String  @id
  username String
  image String
}

model Message {
  id    Int     @id @default(autoincrement())
  text  String
}
