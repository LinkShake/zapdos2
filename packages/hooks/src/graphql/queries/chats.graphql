query Chats($id: String, $params: String) {
  chats(id: $id, params: $params) {
    id
    user1 {
      id
      image
      username
    }
    user2 {
      id
      image
      username
    }
    notifications {
      id
      counter
    }
  }
}
